# Synthetic Regression Baseline

This is the baseline source tree used by the synthetic regression example.

`train.py` trains a small MLP on a fixed deterministic regression target and prints final metrics in a machine-readable format so the example scorer can parse them reliably.
